# ADR-0010：记忆策略与内容治理同源审计

- 状态：Accepted
- 日期：2026-07-20

## 决策

长期记忆的 Markdown 文件继续作为内容事实源，数据库保存索引与不可变操作审计。角色级记忆策略采用版本字段并写入下一次运行快照。管理员纠偏、压缩和删除调用与 Creator 相同的 `MemoryService`，不建立旁路写入口。

“创作决策强制保留”是安全不变式，管理 API 不允许关闭；压缩策略必须保留创作决策、用户偏好和项目禁用词。
